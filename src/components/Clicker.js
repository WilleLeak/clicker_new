import React from 'react'
import { Stack, Typography, Box } from '@mui/material'

const Clicker = () => {
  return (
    <div style={{backgroundColor: 'grey', height: '92vh'}}>
        <Box
        // style = {{height: '100%'}}
        >
            <Typography variant='h3'>
                yarn counter
            </Typography>
        </Box>
    </div>
  )
}

export default Clicker